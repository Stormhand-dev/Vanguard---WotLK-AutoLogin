-- ============================================================
--  AutoLogin/Config.lua
--  Location: Interface/GlueXML/AutoLogin/Config.lua
--
--  Credentials are saved normally in WTF/Config.wtf (default).
--  This file controls only the character selection order.
-- ============================================================

-- Preferred character order on the character selection screen.
-- Use exact character names. The first character in the list
-- is automatically selected on login.
-- Characters not listed appear at the end in their original order.
-- Set to {} to disable.
AUTOLOGIN_CHAR_ORDER = {
    "",
    "",
}
